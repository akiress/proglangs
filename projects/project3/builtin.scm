(define builtin-+ +)
(define (b+ x y) (builtin-+ x y))
(define + "not yet implemented")

(define builtin-- -)
(define (b- x y) (builtin-- x y))
(define - "not yet implemented")

(define builtin-* *)
(define (b* x y) (builtin-* x y))
(define * "not yet implemented")

(define builtin-/ /)
(define (b/ x y) (builtin-/ x y))
(define / "not yet implemented")

(define builtin-= =)
(define (b= x y) (builtin-= x y))
(define = "not yet implemented")

(define builtin-< <)
(define (b< x y) (builtin-< x y))
(define < "not yet implemented")

(define builtin-> >)
(define (b> x y) (builtin-> x y))
(define > "not yet implemented")
